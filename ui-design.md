# UI Design

Quota Wake has no graphical interface.

Its user-facing surfaces are PowerShell command output, structured status
objects, safe error messages, and Windows failure notifications. Keep those
surfaces concise, actionable, noninteractive during scheduled runs, and free of
credentials or raw model payloads.
